import { defineConfig } from 'vite'

export default defineConfig({
  // Base path for standard GitHub Pages: username.github.io/repo/
  // Set to './' to allow the build to work from any relative path
  base: './'
})
